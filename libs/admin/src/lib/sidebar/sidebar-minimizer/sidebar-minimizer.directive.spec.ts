import { SidebarMinimizerDirective } from './sidebar-minimizer.directive';

describe('SidebarMinimizerDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarMinimizerDirective();
    expect(directive).toBeTruthy();
  });
});
