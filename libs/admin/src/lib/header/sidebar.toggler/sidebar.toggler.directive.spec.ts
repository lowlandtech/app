import { SidebarTogglerDirective } from './sidebar.toggler.directive';

describe('Sidebar.TogglerDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarTogglerDirective();
    expect(directive).toBeTruthy();
  });
});
