import { BrandMinimizerDirective } from './brand-minimizer.directive';

describe('BrandMinimizerDirective', () => {
  it('should create an instance', () => {
    const directive = new BrandMinimizerDirective();
    expect(directive).toBeTruthy();
  });
});
