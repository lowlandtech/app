import { AsideTogglerDirective } from './aside.toggler.directive';

describe('AsideTogglerDirective', () => {
  it('should create an instance', () => {
    const directive = new AsideTogglerDirective();
    expect(directive).toBeTruthy();
  });
});
